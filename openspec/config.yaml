# SDD Init — OpenSpec configuration for datos_gob_es_ia
# Auto-generated by sdd-init (2026-06-21)
# Mode: hybrid (Engram + OpenSpec files)

project:
  name: datos_gob_es_ia
  description: "Python project to consume datos.gob.es (Spanish government open data API)"
  status: greenfield
  initial_commit: a7b524a

stack:
  language: Python 3
  package_manager: pip (inferred from .gitignore — no pyproject.toml yet)
  build_system: none
  test_framework: none

testing:
  strict_tdd: false
  reason: "No test runner detected. No test files, no test configs. TDD cannot be enforced without a runner."
  recommended_action: "Install pytest or a test framework at project setup, then set strict_tdd: true"
  test_files_found: []
  test_configs_found: []
  test_config_paths: []

conventions:
  editorconfig: false
  golangci_yml: false
  tsconfig: false
  biome: false
  eslintrc: false
  prettierrc: false
  gitignore: python_template (__pycache__, .venv, *.egg-info, etc.)
  commit_style: conventional (single "Initial commit")

architecture:
  pattern: none (greenfield)
  detected_frameworks: []

persistence:
  mode: hybrid
  engram_save: true
  engram_id: obs-ae3fab37717a766e
  openspec_file: openspec/config.yaml
  skill_registry: .atl/skill-registry.md (exists, cached)

future_work:
  next_recommended: sdd-explore
  notes: "Explore the datos.gob.es API structure, data endpoints, and define project scope before any code"